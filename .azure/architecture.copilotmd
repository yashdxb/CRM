# CRM-Enterprise Azure Architecture

> **Note:** To preview this diagram, install a Mermaid preview extension in your editor.

```mermaid
graph TD
%% Services
svcazureappservice_crm_enterprise_api["`Name: CRM.Enterprise.Api
Path: ../../../server/src/CRM.Enterprise.Api
Language: dotnet
Port: 80`"]
subgraph "Compute Resources"
%% Resources
azureappservice_crm_enterprise_api("`CRM.Enterprise.Api (Azure App Service)`")
end
%% Relationships
svcazureappservice_crm_enterprise_api --> |"hosted on"| azureappservice_crm_enterprise_api
```

**Architecture Explanation:**
- The CRM.Enterprise.Api (.NET) is hosted on Azure App Service.
- All API traffic is routed to this compute resource.
- No additional Azure dependencies (e.g., database, cache, AI) are currently modeled in this diagram.

If you expect a different topology (e.g., database, storage, or other Azure services), let me know and I will update the diagram accordingly.

**Supported Azure hosting technologies:**
- appservice
- function
- containerapp
- staticwebapp
- aks

**Supported Azure bindings:**
- azureaisearch, azureaiservices, appservicemanagedinstance, azureapplicationinsights, azurebotservice, azurecosmosdb, azurekeyvault, azuredatabaseformysql, azureopenai, azuredatabaseforpostgresql, azureprivateendpoint, azurecacheforredis, azuresqldatabase, azurestorageaccount, azureservicebus, azuresignalrservice, azurevirtualnetwork, azurewebpubsub, azurecontainerregistry
