name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  server:
    name: Server Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        if: ${{ hashFiles('server/src/CRM.Enterprise.sln') != '' }}
        run: dotnet restore CRM.Enterprise.sln
        working-directory: server/src

      - name: Build
        if: ${{ hashFiles('server/src/CRM.Enterprise.sln') != '' }}
        run: dotnet build CRM.Enterprise.sln --configuration Release --no-restore
        working-directory: server/src

      - name: Test
        if: ${{ hashFiles('server/src/CRM.Enterprise.sln') != '' }}
        run: dotnet test CRM.Enterprise.sln --configuration Release --no-build
        working-directory: server/src

  client:
    name: Client Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"
          cache-dependency-path: client/package-lock.json

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test -- --watch=false
